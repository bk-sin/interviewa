/**
 * Utils Barrel File
 * Clean imports for all utility functions
 *
 * @example
 * import { preloadAssets, preloadImages } from '@/utils';
 */

export { assetConfig, preloadAssets, preloadImages } from "./asset-loader";
